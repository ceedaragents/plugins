# Cyrus Plugins Marketplace

## Pending: Anthropic Allowlist Approval

This plugin is not yet on Anthropic's approved channels allowlist. Once approved:

1. Update the README — there's an HTML comment with the approved `--channels` command ready to swap in
2. Remove the `--dangerously-load-development-channels` note and replace with the commented-out `--channels` version
